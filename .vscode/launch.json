{
    "configurations": [
        {
            "name": "Containers: Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/support-api/package.json",
                "localRoot": "${workspaceFolder}/support-api"
            }
        }
    ]
}