// import axios from "axios";

// const BASE_URL = "https://boomgefen-job-platform-1.onrender.com/api/v1/email";

// export type EmailData = {
//   to: string;
//   subject: string;
//   text?: string;
//   html?: string;
// };

// export const sendEmail = async (emailData: EmailData) => {
//   const response = await axios.post(`${BASE_URL}/send`, emailData);
//   return response.data; // { success: true, message: 'Email sent' }
// };
